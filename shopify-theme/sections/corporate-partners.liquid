{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="corporate-partners--main section-{{ section.id }}-padding">
  <div class="page-width">
    <div class="corporate-partners--input partners-input--search">
      <label for="searchPartners">
        {% render 'user-icon' %}
        <input id="searchPartners" type="text" placeholder="Search by name, location, etc.">
        <button>{% render 'search-icon' %}</button>
      </label>
    </div>
    <div class="corporate-partners--row">
      <div class="corporate-partners--input partners-input--select">
        <select>
          <option disabled selected>Type of organization</option>
        </select>
      </div>
      <div class="corporate-partners--input partners-input--select">
        <select>
          <option disabled selected>Location</option>
        </select>
      </div>
      <div class="corporate-partners--input partners-input--select">
        <select>
          <option disabled selected>Industry</option>
        </select>
      </div>
      <div class="corporate-partners--input partners-input--select">
        <select>
          <option disabled selected>Projetcs</option>
        </select>
      </div>
    </div>
    <div class="corporate-partners--flex">
      {% for i in (1..3) %}
      <div class="corporate-partners--col">
        <div class="corporate-partners-col--content">
          <img src="https://cdn.shopify.com/s/files/1/0668/2669/9053/files/Group_1.png?v=1702619972" width="auto" height="auto">
          <span>Location</span>
          <h2>Partner Name</h2>
          <p>Short description. Lorem ipsum dolor sit amet, consectetur adipiscing elit. Aenean at tortor ornare, pharetra sem facilisis, lobortis magna. Curabitur massa orci, porttitor ut diam nec, mattis sodales magna. Praesent a neque sed elit fringilla mattis.</p>
          <div class="corporate-partners--link">
            <a href="">View</a>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
    <div class="corporate-partners--mainlink">
      <a href="">View All</a>
    </div>
  </div>
</div>

{% schema %}
  {
    "name": "Section name",
    "settings": [
      {
        "type": "header",
        "content": "t:sections.all.padding.section_padding_heading"
      },
      {
        "type": "range",
        "id": "padding_top",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "t:sections.all.padding.padding_top",
        "default": 36
      },
      {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "t:sections.all.padding.padding_bottom",
        "default": 36
      }
    ]
  }
{% endschema %}
